UAG(DEFAULT) {
    "george"
}

UAG(BAR) {
    "michael"
}

ASG(RO) {
    RULE(1,READ,TRAPWRITE)
}

ASG(SPECIAL) {
    RULE(1,WRITE,TRAPWRITE) {
        UAG(DEFAULT)
        UAG(BAR)
		AUTHORITY("EPICS Root Certificate Authority")
		METHOD("x509")
    }
}
